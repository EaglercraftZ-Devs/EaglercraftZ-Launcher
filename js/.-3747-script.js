// Get a reference to the button element
const button = document.getElementById("playButton"); // Replace "playButton" with the actual ID of your button

// Add an event listener for the click event
button.addEventListener("click", function() {
  // This function will be executed when the button is clicked
  alert("The button was clicked"); // Replace this with your desired action (e.g., redirect to a new page, show/hide content)
});
